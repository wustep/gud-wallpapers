<%#
Partial to conditionally display delete button

Author (11/29/17):: Jason
#%>
<% if signed_in? && (@user.user_rank == 2 || wallpaper.uploader == @user) %>
  <%= button_to "Delete", wallpaper_path(wallpaper), method: :delete, :class => "btn btn-danger btn-small" %>
<% end %>
